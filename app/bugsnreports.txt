// app/login.tsx
import { View, Text, StyleSheet } from 'react-native';

export default function BugsandReportsPage() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>This is the Bugs and Reports Page</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f9f9f9',
  },
  text: {
    fontSize: 24,
    fontWeight: 'bold',
  },
});
